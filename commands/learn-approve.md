---
description: Apply one staged learned-skill proposal
---

Call `learning_pending` with `action=apply` and `id=$1`. Report exactly which skill was created or patched and whether the skill registry reloaded successfully.
